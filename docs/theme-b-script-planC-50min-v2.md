# テーマB 進行原稿 50 分版 v2（セットアップ込み・役割分担なし・現場グループ制）

2026-09-18（金）14:20〜15:10 / 社員向け 50 分 / 講師: 福田

50 分版 v1（`theme-b-script-planC-50min.md`）を次の条件で組み直した。**当日はこの 1 冊だけを見る。**

| 条件 | この原稿での形 |
|---|---|
| 役割分担をしない | 「設計者役」を廃止。仕様書は 31 分に講師が全員に配る。オンライン参加者は仕事の合間に見る人が多いので、**見るだけ**でよい。講師はオンラインに何も求めず、待たない |
| 現場はグループ制 | 現場の参加者は 2〜3 人で 1 台のグループ PC（Node / now-sdk / Claude Code ログイン済み。PDI が 1 つ割り当て済み）。**打つ役**と**読み役**を決め、途中で交代 |
| 事前準備を本編でやる | キットの展開、`now-sdk auth --add`、`setup.ps1`（欠陥版アプリと ATF 設定を各 PDI に入れる）を **3〜12 分のブロックで全員一斉に**行う。`setup.ps1` が走る 4 分間は、講師が「何をしているか」を解説する時間にする |
| アンケート・振り返りは 50 分に入れない | 終了後に別枠で取る。50 分は導入 → セットアップ → 生成 → 実行 → 仕様書 → 赤 → 修正 → まとめ |

**前提として本編の中では行えないもの**: PDI の新規取得（developer.servicenow.com のアカウント作成と払い出しで 10 分以上かかる）、Node / now-sdk / Claude Code のインストール。これらは **グループ PC 側で済ませておく**。PDI を持たない人はグループ PC に入る。

---

## 0. 前提（原稿を読む前に 1 分で確認）

| 項目 | この版での形 |
|---|---|
| 参加者 | 現場のグループ（2〜3 人 × 1 台）。全員が同じ手順を同時に打つ |
| グループ PC の状態（前日までに講師が確認） | Node.js LTS、`npm install -g @servicenow/sdk`、`npm install -g @anthropic-ai/claude-code`（ログイン済み）、PowerShell 実行ポリシー RemoteSigned、ブラウザでその PC の PDI に `admin` でログイン済み。**キットは展開しない**（本編で展開する）。机に PDI の URL と admin パスワードのカード |
| PDI | グループ PC 1 台に 1 つ。**起きている**こと（開始 30 分前に講師がすべてログインして起こす） |
| オンライン参加者 | Zoom で講師の画面を見る。手順は Zoom チャットにも貼るが、講師は待たない。終了後に資料とキットの URL を送る |
| 講師の手元 | キット未展開の状態から参加者と同じ手順を打つ。講師 PDI `dev192510` は既に欠陥版 `x_2221398_todo` が入っているので、`setup.ps1` は「済み」を表示して通り抜ける（実況用）。予備として `ATF Handson 09` に完成品のテスト T1〜T5 |
| 後半の見せ場 | 参加者自身が Claude Code にプロンプト③を打ってアプリを直し、再テストで緑にする。**44 分で終わっていない組は `fix.ps1` に切り替える** |
| 50 分に入れないもの | アンケート、振り返り、講師の学びと反省、投票 |

**記号**

| 記号 | 意味 |
|---|---|
| 🗣 言う | 読み上げてよいセリフ |
| 🖱 講師の操作 | プロジェクターと Zoom で見せる操作 |
| 👥 参加者 | グループに打ってもらう手順。**会場チャットと Zoom チャットの両方に貼る** |
| ✅ 確認 | 次へ進む合図。**現場のグループの挙手だけ**で数える。全グループの 2/3 で進む |
| 🆘 詰まったら | 症状と対処 |
| ⏱ | 時間の目安と逃げ道 |

**講師の画面（開始前に開いておく）**
1. ブラウザ: 講師 PDI に `admin`。タブ A = Handson Todo board、タブ B = Automated Test → Tests 一覧（Application「ATF Handson 09」）
2. ターミナル 1: デスクトップ（キット未展開。3 分に参加者と同じ手順で展開する）
3. 共有しない画面: この原稿、スライド 5 枚、`theme-b/evidence/` のスクリーンショット
4. ブラウザの別タブ: Release ページ https://github.com/Fukuda-Keiju/claude-handson/releases/tag/theme-b-kit-v2（zip の配布元）

**時間割（合計 50 分）**

| 分 | ブロック | 中身 |
|---|---|---|
| 0〜3 | 導入 | 今日やること、用語 4 つ、グループの役割決め |
| 3〜12 | セットアップ | zip 展開 → `auth --add` → `setup.ps1`（走っている間に講師が解説） → `preflight.ps1` |
| 12〜15 | アプリを触る | 欠陥版のボードを自由に操作（空タイトルは講師が触れない） |
| 15〜25 | テスト生成 | プロンプト① → Claude Code が 3 本書く。講師の実況 |
| 25〜31 | 実行 | build → deploy → Run Test 3 本。全部緑 |
| 31〜34 | 仕様書 | 講師が配る。「確かめていない行はどれか」を各グループに探させる |
| 34〜40 | 赤 | プロンプト② → Negative → 赤。4 分類 |
| 40〜48 | 修正 | プロンプト③ → build → deploy → 再テストで緑（44 分で `fix.ps1` 判断） |
| 48〜50 | まとめ | 持ち帰る 3 つ。アンケートと振り返りは終了後 |

---

## 1. 開始前 10 分　入室した人から順に席へ

🖱 **講師の操作**: 開始 30 分前に、グループ PC 全台のブラウザで PDI にログインして起こしておく。Zoom を開き、画面共有を開始（タブ A）。入室した人をグループ PC に案内し、**2 人目が来たら同じ PC に座らせる**。

会場チャットと Zoom チャットに次を貼る。

👥
```
【入室したら】
1. グループ PC の前に座り、同じ PC の人と「打つ役」「読み役（画面の文字を声に出す）」を決めてください。
2. ブラウザに PDI（ServiceNow）の画面が開いていることを確認。閉じていたら机のカードの URL と admin パスワードでログイン。
3. まだ何もダウンロードしなくて大丈夫です。3 分ごろに一斉に始めます。
```

Zoom チャット（Zoom 担当）
```
オンラインの皆さんは講師の画面を見ていてください。手順はこのチャットにも貼りますが、追いかけなくても構いません。
資料とキットの URL は終了後に送ります。
```

🆘
- グループ PC のブラウザが PDI からログアウトしている → カードの URL と admin パスワードで入る。ログイン画面が数分出なければ PDI が休止中。起きるまで待ち、その組は隣の組の画面を見る。
- 1 人しか来ない組ができた → そのまま 1 人で打つ役。読み役は講師が兼ねる。

---

## 2. 0〜3 分　導入（3 分）

🗣 **言う**
「今日は 50 分で、AI に書かせたテストが本当に品質を守ってくれるのかを確かめます。最初の 10 分で、皆さんのグループの PDI に小さなアプリを入れます。そのアプリには欠陥が 1 つ仕込んであります。皆さんは AI にテストを書かせ、それを実行し、欠陥を見つけ、最後は皆さん自身が AI に指示してアプリを直します。他のグループとは一切干渉しません。壊しても大丈夫です。」

🗣 **言う（用語 4 つ。30 秒）**
「言葉を 4 つだけ。ATF は ServiceNow に最初から入っている自動テストの機能で、ボタン一つで動きます。Positive テストは『正しく使えば正しく動く』を確かめるもの。Negative テストは『間違った使い方は拒否される』を確かめるもの。結果は緑が成功、赤が失敗。今日はこれだけ分かれば大丈夫です。」

🗣 **言う（グループの動き方。30 秒）**
「打つ役は私の貼る文をそのままコピーして打つ。読み役は画面に出た文字を声に出す。AI が何をしているか、エラーが何と言っているか、それを組の中で言葉にする係です。生成を待つ 5 分間に手が止まらないのは読み役がいるからです。25 分ごろで役を交代します。」

🖱 **講師の操作**: スライド 1（今日のゴールと流れ）を出す。「ServiceNow を触ったことがある人」を挙手で見る。

---

## 3. 3〜12 分　セットアップ（9 分。全員一斉）

🗣 **言う**
「では環境を作ります。3 手順です。①キットを展開、②自分の PDI への接続を登録、③スクリプトを 1 本走らせる。③が 4 分ほど走るので、その間に何をしているかを説明します。」

### 3-1. キットを展開（1 分）

👥
```
【① キットを展開】ブラウザで次の URL を開き、themeB-kit.zip をダウンロード。
https://github.com/Fukuda-Keiju/claude-handson/releases/tag/theme-b-kit-v2
ダウンロードした zip を右クリック → 「すべて展開」→ 展開先を  デスクトップ  にして展開。
デスクトップに themeB-kit フォルダができたら、その中（setup.ps1 が見える場所）を開き、何もない所で右クリック → 「ターミナルで開く」。
開いたターミナルで、まず次の 1 行を打つ（ダウンロードしたファイルに付く「ブロック」を外す。出力は何も出ません）。
  Get-ChildItem -Recurse *.ps1 | Unblock-File
```

🆘
- 展開後に `themeB-kit\themeB-kit` と二重になっている → 内側（`setup.ps1` がある方）で「ターミナルで開く」。
- `Unblock-File` を打ち忘れて `setup.ps1` が「デジタル署名されていません」「UnauthorizedAccess」で止まった → 上の 1 行を打ってから、もう一度 `.\setup.ps1`。または `powershell -ExecutionPolicy Bypass -File .\setup.ps1`。**2026-09-17 のパイロット PC で実際に発生**（ブラウザで落とした zip を Windows 標準で展開すると必ず付く）。

🖱 **講師の操作**: 同じ手順を画面でやる。展開後のフォルダの中身を見せる。「todo-app がテスト対象のアプリの中身、atf-tests がこれからテストを書かせる場所、fix は後半で使います。中は今は見ないでください。」

### 3-2. 接続を登録（2 分）

👥
```
【② PDI への接続を登録】開いたターミナルで、次の 1 行を打つ（URL は机のカードのもの。devの後の数字を自分の PDI に）。
  now-sdk auth --add https://devXXXXXX.service-now.com --type basic --alias mypdi
聞かれたら順に答える:  ユーザー名 admin → パスワード（カードのもの。打っても画面に出ません）→ Enter。
「Successfully」のような文字が出れば OK。
確認:  now-sdk auth --list  と打って、[mypdi] の行に自分の URL が出ること。
```

🗣 **言う**
「`now-sdk` は ServiceNow の SDK のコマンドです。ここで登録した接続先を、この後のスクリプトも Claude Code も使います。パスワードは PC の中にだけ保存され、キットのフォルダには入りません。」

🆘
- 「スクリプトの実行が無効」と赤字 → `Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned` → `Y` → もう一度。
- `now-sdk` が見つからない → グループ PC の準備漏れ。`npm install -g @servicenow/sdk`（1〜2 分）。その間に隣の組の画面を見る。
- パスワードを打ち間違えた → `now-sdk auth --delete mypdi` のあと、もう一度 `--add`。

### 3-3. setup.ps1 を走らせる（4〜5 分。講師の解説はこの間）

👥
```
【③ アプリを自分の PDI に入れる】同じターミナルで
  .\setup.ps1
と打って Enter。4〜5 分走ります。緑の [OK] が順に増え、最後に「準備完了です」と出れば成功。
赤の [NG] で止まったら、その行を読み役が声に出して講師を呼んでください。
走っている間、画面を見ながら講師の説明を聞いてください。
（「デジタル署名されていません」と赤く出たら:  powershell -ExecutionPolicy Bypass -File .\setup.ps1  で実行）
```

🖱 **講師の操作**: 講師も `.\setup.ps1` を打つ（講師 PDI は既に入っているので「済み」で通り抜ける。参加者の画面と同じログが流れるのは build の工程）。Zoom 共有をターミナルに。**参加者のログが進むのに合わせて**次を話す。

🗣 **解説（setup.ps1 のログに合わせて）**

`会社コードの取得` が出たとき:
「まずスクリプトが皆さんの PDI に『会社コード』を聞いています。PDI には 1 つずつ 7 桁の番号が振られていて、そこで作るアプリの名前は全部 `x_番号_` で始まらないといけません。今日のアプリは私の PDI で作ったので、私の番号が付いています。それをそのまま皆さんの PDI に入れようとすると、ServiceNow は『他社のアプリだ』と言って拒否します。実際に試して、拒否されました。」

`スコープ接頭辞の書き換え` が出たとき:
「なので、アプリの中の私の番号を、皆さんの番号に全部書き換えています。20 ファイル、45 か所。1 秒で終わります。この後 AI に読ませる説明書も、皆さんの番号に書き換わっています。」

`スコープ登録` が出たとき:
「アプリの ID を皆さんの PDI で採っています。ここでアプリの『箱』が予約されます。todo-app がテスト対象、atf-tests がテストの箱。テストをアプリと別の箱に入れるのは、テストを消してもアプリに影響しないようにするためです。」

`npm ci (todo-app)` が出たとき（**ここが一番長い。スライド 2「今日のアプリ」を出す**）:
「今は部品をダウンロードしています。この間にアプリの説明を。Handson Todo board という、タスクを Open・In Progress・Done の 3 列で管理する小さなアプリです。タスクの入れ物になるテーブルが 1 つ、保存のときにサーバーで動く処理（Business Rule）が 2 つ、画面が 1 つ。これを ServiceNow の SDK で、TypeScript のコードとして書いてあります。皆さんが直すのもこのコードです。」

`build (todo-app)` が出たとき:
「build はコードを ServiceNow が読める形に変換する工程。型の間違いはここで止まります。1 分ほど。」

`deploy (todo-app)` が出たとき:
「deploy が、変換したアプリを皆さんの PDI に送り込む工程です。これが終わると、皆さんの PDI のメニューに Handson というアプリが現れます。」

`npm ci (atf-tests)` `build (atf-tests)` が出たとき:
「テストの箱の方も同じ準備をしています。中身はまだ空です。15 分に AI に書かせます。」

`ATF 実行設定を true にする` が出たとき:
「最後に、PDI の設定を 2 つ切り替えています。PDI は最初、自動テストの実行が止められているので、それを許可しています。手でやると忘れる項目なので、スクリプトに入れました。」

### 3-4. 確認（1 分）

👥
```
【④ 確認】「準備完了です」と出たら、続けて
  .\preflight.ps1
と打つ。全部 [OK] なら手を挙げてください。
```

✅ **確認**（12 分）: グループの挙手を数える。2/3 で進む。**残りの組には講師が席まで行き、13 分までに直らなければ「隣の組と一緒に見て、15 分の生成から自分の PC で追いつく」と伝える。**

⏱ 12 分になっても `setup.ps1` が走っている組は、そのまま走らせておいて 3 章に進む（アプリを触るのは講師の画面で見ればよい）。15 分のプロンプト①までに終わっていれば追いつける。

🆘
- 会社コードを読めない → PDI が休止中かログインが違う。ブラウザで PDI にログインし直し、`.\setup.ps1` をもう一度（済んだ工程は飛ぶ）。
- `Unable to install application as application was null` → 接続先が別の PDI を指している（カードの URL と `now-sdk auth --list` を見比べる）。直して `.\setup.ps1 -SkipNpm`。
- `npm ci` でネットワークエラー → Wi-Fi を確認して `.\setup.ps1`（済んだ工程は飛ぶ）。
- 12 分を大きく過ぎても終わらない組 → その組は講師のキット（講師 PC の第 2 ターミナル）を使わせず、**隣の組に合流**。1 台に 4〜5 人になっても進行を優先する。

---

## 4. 12〜15 分　アプリを触る（3 分。仕様書はまだ渡さない）

🗣 **言う**
「アプリが入りました。2 分、自由に触ってください。タスクを作る、列を動かす、開く、消す。読み役は何が起きたか声に出してください。」

🖱 **講師の操作**（**空タイトルの保存は絶対に試さない**）
1. タブ A で左上 **All** → `Handson` → **Handson Todo board**。
2. **New** → タイトル `講師のタスク` → **Save**。一覧に出る。
3. ボードに戻り、矢印ボタンで In Progress へ。

👥
```
【アプリを触る】PDI のブラウザで、左上 All → 検索欄に Handson → Handson Todo board。
2 分、自由に触ってください。タスクを作る、列を動かす、開く、消す。
```

🗣 **言う（締め）**
「質問です。このアプリの今の動きを守るテストを書くなら、何を確かめますか。組の中で 1 つ言ってみてください。……それをこれから AI に頼みます。」

---

## 5. 15〜25 分　Claude Code でテスト生成 ＋ 講師の実況（10 分）

🗣 **言う**
「Claude Code にテストを 3 本書かせます。文面はチャットに貼るので、そのまま貼り付けてください。5〜8 分かかります。その間、私も同じことをやります。」

👥
```
【Claude Code を起動】themeB-kit\atf-tests フォルダを開き、何もない所で右クリック → 「ターミナルで開く」。
  claude  と打って Enter。「このフォルダを信頼しますか」は Yes。入力欄が出たら次へ。
```

👥
```
【プロンプト①】claude の入力欄に、下の文をそのまま貼って Enter。
途中で「ファイルを作成してよいか」「npm run build を実行してよいか」と聞かれたら Yes。
「build が通りました」のような報告が出るまで待つ。deploy はまだしない。
```
続けて貼る:
```
このプロジェクトは ServiceNow Fluent SDK の ATF テスト専用プロジェクトです。
テスト対象は同じインスタンス上の別スコープのアプリ「Handson Todo」です。
スコープ名・テーブル名・画面の URL は CLAUDE.md の「テスト対象アプリ」の節に書いてあります。それを使ってください。
CLAUDE.md のルールに従って、次の 3 本の ATF テストと、それをまとめた TestSuite を src/fluent/atf/ に作ってください。
1. Positive（サーバー）: タイトル付きでレコードを作成でき、state の初期値が open であること
2. Positive（UI）: ボード画面で New を押し、タイトルを入力して Save すると一覧に表示されること
3. 今のアプリの挙動を確認したうえで、もう 1 本、あなたが妥当だと思うテストを追加する
作り終えたら npm run build が通ることを確認してください。deploy はしないでください。
```

🖱 **講師の操作**: 講師のキットの `atf-tests` で `claude` を起動し、同じプロンプト①を貼る。Zoom 共有をこのターミナルに。

🗣 **実況（進み方に合わせて順番は前後してよい）**

CLAUDE.md を読んでいるとき:
「AI がまずプロジェクトの説明書を読んでいます。ATF の書き方、ボタンの名前、そして皆さんの会社コードに書き換えられたアプリ名。さっきのスクリプトがこの説明書を書き換えたので、AI は皆さんの PDI のアプリを正しく指せます。」

ファイルを作り始めたとき（**スライド 3「ATF のテストとは」**）:
「ATF のテストは『ステップ』の並びです。レコードを 1 件作る、値が open か確かめる、画面で New を押す。それぞれ 1 ステップ。今できている `.now.ts` というファイル 1 つが、ServiceNow の画面のテスト 1 つになります。」

3 本目のファイルができたとき（**必ず中身を読み上げる**）:
「3 本目は、AI に『妥当だと思うテストを追加して』と頼んだものです。……（画面を読む）……『〇〇すると〇〇になる』ことを確かめています。今のアプリの動きをそのまま確かめるテストですね。皆さんの 3 本目も似た形のはずです。これでいいかどうか、31 分に戻ってきます。」

`npm run build` が走っているとき（**スライド 4「Positive と Negative」**）:
「テストには 2 種類。正しく使ったら正しく動く、が Positive。間違った使い方が拒否される、が Negative。AI は Positive が得意です。Negative は、何が間違いかを知らないと書けません。この違いを後半で見ます。」

🗣 **待ち時間の問い（参加者の手が止まっているとき）**
「待っている間に組の中で 1 つ。このアプリで『やってはいけない操作』は何だと思いますか。30 秒。」（答えは拾わない。31 分の伏線）

✅ **確認**（24 分）: 「『build が通りました』と出た組、挙手。」2/3 で進む。

⏱ 25 分になったら、終わっていない組には次を貼って先へ進む。

🆘
```
【Checkpoint】claude を Esc で止めて構いません。
themeB-kit\atf-tests\src\fluent\atf\ の中にできかけのファイルがあれば全部削除し、
themeB-kit\checkpoint フォルダの中の全ファイルを、その atf フォルダにコピーしてください（スコープ名は setup.ps1 が書き換え済み）。
そのまま次の【送る】へ進めます。
```
- `claude` が「ログインしてください」→ グループ PC の準備漏れ。指示どおりログイン。1 分で入れなければ Checkpoint で合流。
- 講師の生成が遅い → 実況を切り上げ、`themeB-kit\checkpoint\t1-insert-open.now.ts` を開いて読み上げに切り替える。講師も Checkpoint で先へ。

---

## 6. 25〜31 分　build → deploy → Run Test（6 分。全部緑になる予定）

🗣 **言う**
「できたテストを自分の PDI に送り、ServiceNow の画面からボタンで実行します。ここで打つ役と読み役を交代してください。」

👥
```
【送る・打つ】claude を Esc で一度抜けるか、別のターミナルを atf-tests で開いて、次を順に打つ。
  npm run build
  npm run deploy
「Installed」や「success」が出れば成功。1〜2 分かかります。
```

```
【実行する・見て押す】PDI のブラウザで、左上 All → 検索欄に Automated Test → 「Tests」。
一覧の上の絞り込みで Application 列に  ATF Handson  と入れる。自分のテストが 3 本出ます。出なければ F5。
1 本目の名前をクリック → 右上「Run Test」→ 小さな画面の「Run Test」。
「Client Test Runner」という新しいタブが自動で開きます。閉じないでください。
元のタブに戻ると結果画面になり、Status が Success（緑）になります。
残り 2 本も同じように実行。3 本とも緑になったら手を挙げてください。
```

🖱 **講師の操作**（Zoom 共有をブラウザへ。押すたびに名前を声に出す）
1. タブ B（Tests 一覧）。ライブ生成が失敗していたら「ATF Handson 09」の `Todo: タイトル付きで作成すると state=open になる` を使う。
2. 1 本目を開き、**Run Test** → **Run Test**。
3. 新しいタブを見せる。「これが Client Test Runner。UI テストはこのタブの中で動きます。閉じると止まります。」
4. 結果画面。「上に Status、下に Test Result Items。ステップごとの結果。緑が成功。」
5. UI テストのときは Runner タブを映す。「人間の代わりにブラウザが動いています。皆さんの画面でも同じことが起きているはずです。」

✅ **確認**（30 分）: 挙手。2/3 で進む。

🗣 **言う（締め）**
「全員、緑ですね。ここで質問です。テストが全部通りました。このアプリは正しいと言えますか。組の中で 10 秒、話してください。」

🆘
- `npm run deploy` が赤字 → 読み役に先頭 3 行を読ませる。`auth`/`401` → `now-sdk auth --list`。`scopeId` → `.\setup.ps1 -SkipNpm -SkipInstall`（1 分）。
- 「Waiting for a test runner」のまま → ポップアップブロックを許可して再度 Run Test。または All → Automated Test Framework → Client Test Runner を手で開く。
- UI テストだけ動かない → キットのフォルダで `node set-atf-props.mjs mypdi`。
- UI テストだけ赤（要素が見つからない）→ 「テスト側の問題の可能性が高いです。今は先に進みます」。サーバー側 2 本が緑なら進む。

---

## 7. 31〜34 分　仕様書を配る（3 分。講師が配り、参加者が穴を探す）

🗣 **言う**
「では、このアプリの仕様書です。5 行だけ。」

🖱 **講師の操作**: スライドに仕様書 5 行を出し、両チャットに貼る。

👥
```
【仕様書】
Handson Todo 仕様（抜粋）
1. タスクにはタイトルが必須である。空欄では新規作成も保存もできず、エラーが表示される。
2. タイトルは 100 文字以内である。
3. 状態は Open / In Progress / Done の 3 つである。
4. 状態を Done にすると completed_at に日時が自動で入る。
5. ボード画面ではカードを隣の列にだけ移動できる。
```

🗣 **言う**
「組の中で 1 分。自分たちの 3 本のテストと、この 5 行を見比べてください。**仕様に書いてあるのに、確かめていない行はどれですか。** 特に 3 本目は、仕様のどれを確かめていますか。」

🖱 **講師の操作**: 1 分待つ。**1 組だけ**指名して答えてもらう（「1 番、空欄のテストがない」が出る。出なければ講師が言う）。

🗣 **言う**
「『タイトル必須なのに、空欄のテストがない』。なぜ AI は書かなかったか。AI は『今のアプリの動き』を見て、その通りに動くことを確かめるテストを書きました。3 本目がそれです。もしアプリに欠陥があれば、欠陥もそのまま正解として写し取られます。これが今日の一番大事な話です。プロダクトを見てテストを作ると、欠陥が正解になる。だからテストは仕様から作る。では仕様を渡して AI に直させます。」

👥
```
【仕様書を保存・打つ】themeB-kit\atf-tests\docs フォルダに spec.md という名前のファイルを作り、上の 5 行を貼って保存。
（メモ帳で新規作成 → 貼る → 名前を付けて保存 → ファイル名 spec.md、種類「すべてのファイル」）
```

---

## 8. 34〜40 分　テストを仕様に合わせて直す → 赤を出す（6 分）

👥
```
【プロンプト②】atf-tests のターミナルで claude を起動し（claude と打つ）、下の文をそのまま貼って Enter。
聞かれたら Yes。「build が通りました」まで待つ。
```
続けて貼る:
```
docs/spec.md に仕様書を置きました。仕様と今のテストを照らし合わせ、
仕様で禁止されている操作が拒否されることを確認する Negative Test を 1 本追加してください。
既存テストの期待値が仕様と矛盾している場合は仕様に合わせて直してください。
npm run build が通ることを確認してください。deploy はしないでください。
```

```
【送って実行】終わったら
  npm run build
  npm run deploy
PDI の Tests 一覧を F5。新しく増えた Negative のテストを開き、Run Test → Run Test。
今度は Status が Failure（赤）になります。赤になったら手を挙げてください。
```

🖱 **講師の操作**: 講師も同じことを実行し、Zoom にターミナル → ブラウザの順で映す。赤の結果画面を出す。間に合わなければ「ATF Handson 09」の `Todo: 空タイトルはサーバーで拒否される (Negative)` を Run Test して赤を見せる。

🗣 **言う（赤が出たら。スライド 5「4 分類」）**
「赤が出ました。テストが赤になる原因は 4 つのどれかです。1 つ目、アプリの欠陥。これが本来見つけたいもの。2 つ目、テストの間違い。ボタンの名前が違う、期待値が逆。3 つ目、環境。テスト用のタブを閉じた、ログインが切れた。4 つ目、データ。他のレコードと混ざった。今日はどれだと思いますか。指で番号を。」（会場の指を見て多い番号を言う。20 秒）

🗣 **言う**
「確かめましょう。結果画面の下、Test Result Items。赤いステップをクリックすると Output という欄があります。」

🖱 **講師の操作**: 赤いステップをクリックし、Output の「Inserted record ... when insert was expected to fail」を読む。

🗣 **言う**
「『レコードが作成された』とあります。テストは『空欄なら保存できないはず』と言い、アプリは保存してしまった。1 番、アプリの欠陥です。テストがアプリの仕様違反を見つけました。」

✅ **確認**（39 分）: 挙手。

🆘
- Negative が緑になる → その組のテストが「保存できること」を期待している。「AI がまた今の動きを正解にした例です」と紹介し、次を貼る。
  ```
  【Solution】themeB-kit\solution の t4-empty-title-server.now.ts だけを atf-tests\src\fluent\atf\ にコピー
  （todo-suites.now.ts はコピーしない）。npm run build → npm run deploy → Tests 一覧を F5 → 「空タイトルはサーバーで拒否される (Negative)」を Run Test。
  ```
- 39 分を過ぎても生成が終わらない組 → 上の【Solution】を貼って先へ。

---

## 9. 40〜48 分　参加者自身がアプリを直す → 再テスト（8 分。見せ場）

🗣 **言う**
「テストが欠陥を見つけました。直すのは開発者の仕事です。今日は皆さん自身が直します。AI に渡すのは 2 つ。守られていない仕様の 1 行と、赤くなったテストの Output。アプリのソースはキットの todo-app フォルダにあります。」

👥
```
【直す・打つ】themeB-kit\todo-app フォルダを開き、何もない所で右クリック → 「ターミナルで開く」。
（atf-tests ではありません。アプリ本体のフォルダです）
claude と打って Enter。「信頼しますか」は Yes。
下のプロンプト③をそのまま貼って Enter。聞かれたら Yes。「build が通りました」まで待つ（3〜4 分）。
```
続けて貼る（**プロンプト③**）:
```
このプロジェクトは ServiceNow Fluent SDK で作ったアプリ「Handson Todo」です。
次の仕様が守られていません。
  仕様 1: タスクにはタイトルが必須である。空欄では新規作成も保存もできず、エラーが表示される。
ATF の Negative Test「空タイトルで insert すると失敗するはず」が赤になりました。Output は次のとおりです。
  Inserted record ... when insert was expected to fail
サーバー側・画面側・テーブル定義の 3 か所で仕様 1 が守られるように直してください。
- サーバー側: Business Rule のスクリプト（src/server/business-rules/validate-title.ts）で、前後の空白を除いたタイトルが空なら
  gs.addErrorMessage('タイトルは必須です') を出し、current.setAbortAction(true) で保存を中止する
- 画面側: 作成フォーム（src/client/components/TodoForm.tsx）で、タイトルが空のまま Save されたら
  'タイトルは必須です' をエラーとして表示し、サーバーに送らない
- テーブル定義: src/fluent/tables/todo-item.now.ts の title を mandatory: true にする
既存の動き（空白の前後を整える処理、他の列、デモデータ）は変えないでください。
終わったら、変更したファイルと変更点を短く一覧にして見せてください。
npm run build が通ることを確認してください。deploy はしないでください。
```

🖱 **講師の操作**: 講師も `todo-app` で `claude` を起動し、同じプロンプト③を貼る。Zoom 共有をこのターミナルに。

🗣 **実況**

`validate-title.ts` を書き換えたとき（**必ず画面で読む**）:
「サーバー側です。ここが本命。`trimmed === ''`、空白を取ったタイトルが空なら。`gs.addErrorMessage('タイトルは必須です')`、エラーを出して。`current.setAbortAction(true)`、保存そのものを止める。この 3 行が欠陥版にはありませんでした。」

`TodoForm.tsx` と `todo-item.now.ts` を書き換えたとき:
「画面側の守りと、テーブル定義の `mandatory: true`。合わせて 3 か所、差は十数行。テストがなければ誰も気づきませんでした。」

`npm run build` が走っているとき:
「なぜサーバー側が本命か。画面のチェックだけだと、API やスクリプトから入れたレコードは素通りします。だから保存の直前にサーバーでもう一度確かめる。」

⏱ **44 分の判断（この原稿で一番大事な分岐）**: 「『build が通りました』と出た組、挙手。」半分に届いていなければ、**全員に予備を貼って切り替える**。届いていれば、出ていない組にだけ貼る。

🆘（44 分の予備）
```
【予備・打つ】claude を Esc で止め、themeB-kit フォルダ（todo-app の 1 つ上）を「ターミナルで開く」→
  .\fix.ps1
  cd todo-app
  npm run build
  npm run deploy
fix.ps1 は同じ 3 ファイルの完成版を上書きします。中身は AI が書こうとしていたものと同じです。
```
「AI の修正は時間がかかることがあるので、用意してある同じ修正を入れます」と言う。

👥
```
【送る・打つ】claude を Esc で抜けるか、別のターミナルを todo-app で開いて
  npm run build
  npm run deploy
「Installed」や「success」が出るまで待つ（1 分ほど）。
```

👥
```
【再テスト・見て押す】PDI の Tests 一覧で
1. Negative のテストを開いて Run Test → Run Test。今度は緑（Success）になるはず。
2. 続けて、最初の Positive のうちサーバー側の 1 本（タイトル付きで作成 → open）も実行。緑のままのはず。
2 本とも緑になったら手を挙げてください。
```

🗣 **言う（挙手が半分を超えたら）**
「Negative が緑になった。それだけでは足りません。Positive が緑のままであることも確認しました。直したつもりで別の所を壊していないか、これを回帰確認と言います。テストが自動になっていると、ボタン 1 つでできます。皆さんは今日、環境を作り、AI にテストを書かせて欠陥を見つけ、AI に指示して直し、再テストで確かめる、この一巡りを 45 分で終えました。皆さんがしたのは『何を渡すか』を決めることだけです。」

⏱ 実測（2026-09-16）: プロンプト③ 116 秒、build 13〜23 秒、deploy 17〜20 秒。**47 分になったら再テストは Negative 1 本だけにする。**

🆘
- `fix.ps1` が「setup.ps1 がまだ実行されていません」→ 別のフォルダで打っている。デスクトップの `themeB-kit` を開き直す。
- deploy が `scopeId` で失敗 → `.\setup.ps1 -SkipNpm`（1 分半）。時間がなければ講師の画面で結果を見てもらう。
- 再テストしても Negative が赤 → deploy 完了前に押している。30 秒待って再度。Output が「Inserted record」なら `validate-title.ts` に `setAbortAction` があるか見る。無ければ `fix.ps1`。
- Positive が赤 → AI が既存の動きを壊した。「回帰確認が要る場面そのものです」と紹介し、`fix.ps1` で上書きして再 deploy。
- 講師の deploy が失敗 → 「修正版は事前に確認済みです」と言い、`theme-b/evidence/suite-result-fix-5pass.png` を出す。

---

## 10. 48〜50 分　まとめ（2 分）

🗣 **締めのセリフ（スライド 6）**
「まとめます。AI はテストを速く書けます。今日 3 本を 8 分で書きました。でも、何が正しいかを決めるのは仕様です。AI はそれを知りません。仕様書を渡すまで、空欄のテストは 1 本も出てきませんでした。その差を作ったのは紙 1 枚です。そして皆さんは、テストが見つけた欠陥を自分の手で直し、再テストで確かめました。持ち帰ってほしいのは 3 つ。1 つ、仕様を先に渡す。2 つ、Negative を必ず頼む。3 つ、赤が出たら 4 分類で考える。」

🗣 **言う（10 秒）**
「今日作ったテストと直したアプリは、皆さんの PDI とデスクトップの themeB-kit に残っています。資料とキットの URL はこの後チャットに貼ります。振り返りとアンケートはこの後の時間で。ありがとうございました。」

⏱ 49 分 30 秒で必ず終える。

---

## 11. 時間が押したときの捨て順（v2）

1. 4 章（アプリを触る）を 1 分にする。
2. 8 章のプロンプト②をやめ、Solution の T4 をコピーして deploy → Run Test だけにする（3 分短縮）。
3. 9 章のプロンプト③を最初から `fix.ps1` にする（3 分短縮。参加者が自分で直す体験は残る）。
4. 9 章の再テストを Negative 1 本だけにする。
5. 7 章の「組で穴を探す 1 分」をやめ、講師が言う。
6. 9 章の参加者による修正を全部やめ、講師の実演だけにする。見せ場なので、3・4 番でも間に合わないときだけ。

**セットアップ（3 章）は削れない。** ここが押したら、4 章を切り、5 章のプロンプト①を 15 分ちょうどに貼って生成の待ち時間で吸収する。

逆に余ったら: 9 章のあとに、仕様書 2〜5 番から 1 つ選び、参加者に Negative をもう 1 本書かせる。

---

## 12. 困ったとき早見表（v2）

| 症状 | まず見る | 対処 |
|---|---|---|
| 3 章: `now-sdk` / `claude` が見つからない | グループ PC の準備漏れ | `npm install -g @servicenow/sdk` / `npm install -g @anthropic-ai/claude-code`。その間は隣の組の画面を見る |
| 3 章: 「スクリプトの実行が無効」 | 実行ポリシー | `Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned` → `Y` |
| 3 章: 会社コードを読めない | PDI が休止 / 接続先違い | ブラウザで PDI にログイン → `.\setup.ps1` をもう一度（済んだ工程は飛ぶ） |
| 3 章: `Unable to install application as application was null` | 接続先が別の PDI | `now-sdk auth --list` とカードの URL を見比べる。直して `.\setup.ps1 -SkipNpm` |
| 3 章: 13 分を過ぎても終わらない | | その組は隣の組に合流。15 分以降に自分の PC で追いつく |
| 5 章: `claude` が「ログインしてください」 | グループ PC の準備漏れ | 指示どおりログイン。1 分で入れなければ Checkpoint |
| 6 章: `npm run deploy` で `scopeId` がない | `now.config.json` | `.\setup.ps1 -SkipNpm -SkipInstall`（atf-tests）/ `.\setup.ps1 -SkipNpm`（todo-app） |
| 6 章: Run Test で Runner が開かない | ポップアップブロック | 許可して再度。または All → Automated Test Framework → Client Test Runner |
| 6 章: UI テストが Pending のまま | `sn_atf.runner.enabled` | キットのフォルダで `node set-atf-props.mjs mypdi` |
| 8 章: Negative が緑になる | テストが「保存できる」を期待 | Solution の T4 を渡す |
| 9 章: プロンプト③が 44 分までに終わらない | | 全員 `fix.ps1` に切り替える（迷わない） |
| 9 章: 修正後に Positive が赤 | AI が既存の動きを壊した | 回帰確認の実例として紹介し、`fix.ps1` で上書き |
| 全般: 他の組と結果が違う | 各組の PDI なので当然 | 欠陥版が入っているか、ボードで空タイトル保存を試させて確認（**34 分以降だけ**） |
| 全般: Zoom の画面共有が止まる | | 会場を優先して進める。Zoom 担当がチャットで実況し、復旧後に再開 |

---

## 13. 当日チャットに貼る文の一覧（コピー元）

| 時刻 | 見出し | 本文の場所 |
|---|---|---|
| 開始前 | 【入室したら】 | 1 章 |
| 3 分 | 【① キットを展開】 | 3-1 |
| 4 分 | 【② PDI への接続を登録】 | 3-2 |
| 6 分 | 【③ アプリを自分の PDI に入れる】 | 3-3 |
| 11 分 | 【④ 確認】 | 3-4 |
| 12 分 | 【アプリを触る】 | 4 章 |
| 15 分 | 【Claude Code を起動】【プロンプト①】+ 本文 | 5 章 |
| 25 分（予備） | 【Checkpoint】 | 5 章 🆘 |
| 25 分 | 【送る・打つ】【実行する・見て押す】 | 6 章 |
| 31 分 | 【仕様書】【仕様書を保存・打つ】 | 7 章 |
| 34 分 | 【プロンプト②】+ 本文、【送って実行】 | 8 章 |
| 39 分（予備） | 【Solution】 | 8 章 🆘 |
| 40 分 | 【直す・打つ】+ プロンプト③本文 | 9 章 |
| 44 分（予備） | 【予備・打つ】（`fix.ps1`） | 9 章 🆘 |
| 45 分 | 【送る・打つ】 | 9 章 |
| 46 分 | 【再テスト・見て押す】 | 9 章 |
| 50 分 | 資料とキットの URL | 10 章末 |

---

## 14. 前日までに講師がやること（この版で変わった点）

| # | 項目 | 備考 |
|---|---|---|
| 1 | グループ PC 全台に Node / now-sdk / Claude Code（ログイン済み）/ 実行ポリシー | **キットは展開しない**。`now-sdk auth` も登録しない（本編で参加者がやる） |
| 2 | グループ PC 1 台につき PDI 1 つ。机に URL と admin パスワードのカード | 会社コードが講師と違う PDI であること |
| 3 | 1 台で本編 3 章を最初から最後まで通し、`setup.ps1` の所要時間を計る | 4〜5 分に収まるか。収まらなければ 3 章を 10 分に延ばし、4 章を削る |
| 4 | 通した PC は **キットを消し、`now-sdk auth --delete mypdi`、PDI 側のアプリ 2 つ（Handson Todo / ATF Handson）を Delete** して元に戻す | 本番で「既にあります」と出ないように |
| 5 | Release の zip が最新（`build-kit.ps1` → `gh release upload --clobber`） | |
| 6 | 講師 PDI: 欠陥版 `x_2221398_todo`、`ATF Handson 09` に T1〜T5、検証用アプリを Delete 済み | |
| 7 | スライドを 6 枚に（今日のゴール / 今日のアプリ / ATF のテストとは / Positive と Negative / 4 分類 / まとめ） | v1 の「役割」スライドを「今日のアプリ」に差し替え |

---

## 15. v1（50 分・役割分担あり）との差分

| v1 | v2 | 理由 |
|---|---|---|
| 事前課題で setup 済み。本編は preflight から | **本編 3〜12 分で zip 展開 → auth → setup.ps1** | 事前準備を本編でやる |
| 設計者役（オンライン）が仕様書を持ち、30 分に見落としを指摘 | 役割なし。**講師が 31 分に配り、各組が自分のテストと見比べて穴を探す** | 役割分担を廃止。オンラインは見るだけ |
| 振り返り 3 分 + 講師の一言 2 分 | **まとめ 2 分のみ** | アンケート・振り返りは終了後 |
| 投票 1 回（Zoom 併用） | 会場の指のみ（4 分類） | オンラインに求めない |
| 自分の PC + 共用 PC 3 台 | **グループ PC のみ**（2〜3 人 × 1 台） | 現場は全員グループ制 |
| ✅ は会場の挙手 6 割 | ✅ は **グループの挙手 2/3** | 数える単位が人からグループへ |
| 早見表に共用 PC の行 | 早見表に **3 章（セットアップ）の行** | 詰まりどころが変わった |
