# テーマB 進行原稿 案C・50 分版（自分の PDI + 会場共用 PC 3 台、ハンズオン優先）

2026-09-18（金）14:20〜15:10 / 社員向け 50 分 / 講師: 福田

60 分版（`theme-b-script-planC.md`）を次の 3 条件で組み直した原稿。**当日はこの 1 冊だけを見る。**

| 条件 | この原稿での形 |
|---|---|
| 持ち時間 50 分 | 10 分削る。削ったのは「投票 1・3」「設計者役の発言待ち」「振り返りの読み上げ」「講師の学びと反省（5 分 → 2 分）」。ハンズオンの 4 ブロック（生成・実行・赤・修正）は残す |
| 会場に共用 PC 3 台 | 事前準備を終えた PC を **A・B・C** の 3 台用意する。自前の環境がない人はここに座り、**2 人 1 台**で参加する。共用 PC は講師が前日までに `setup.ps1` を通し、ログイン済みにしておく |
| ハンズオン優先 | 進行の合図（✅）は **会場の手を動かす人だけ**で数える。オンラインの参加者は自分のペースで追いかけ、講師は待たない。オンライン視聴者（設計者役）への問いかけは Zoom 担当に任せ、講師は返事を待たずに進む |

---

## 0. 前提（原稿を読む前に 1 分で確認）

| 項目 | この版での形 |
|---|---|
| アプリの置き場所 | 参加者ごとの PDI に 1 つずつ。事前課題で `setup.ps1` が欠陥版を入れてある。共用 PC A/B/C も同じ（それぞれ別の PDI） |
| テストの置き場所 | 各 PDI のスコープ `x_<会社コード>_atf`（アプリ名 **ATF Handson**） |
| 手を動かす人（テスター兼開発者） | ① 事前課題を終えて自分の PC で参加する人 ② 共用 PC A/B/C に座る人（2 人 1 台。**打つ役**と**読み役**） ③ オンラインで事前課題を終えた人（自分のペース。講師は待たない） |
| 見る人（設計者役） | オンラインで環境がない人。仕様書を事前に持っている。Zoom 担当が相手をする。**講師は進行を止めて意見を待たない** |
| 共用 PC の席順 | 会場に来た人のうち自前の環境がない人から順に A → B → C。3 台が埋まったら、あとから来た環境なしの人は「読み役」として空いている席の 3 人目に入るか、オンライン視聴者と同じ扱い |
| 講師の手元 | キット展開フォルダ（参加者と同じ構成）、講師 PDI `dev192510` に欠陥版 `x_2221398_todo` と空の `atf10` |
| 後半の見せ場 | 参加者自身が Claude Code にプロンプト③を打ってアプリを直し、再テストで緑にする。**41 分で終わっていない人は迷わず `fix.ps1` に切り替える** |
| アンケート・投票 | 投票は 1 回だけ（赤の 4 分類。会場は指で番号、Zoom は投票機能）。アンケートは終了後に URL を貼るだけ |

**記号**

| 記号 | 意味 |
|---|---|
| 🗣 言う | 読み上げてよいセリフ |
| 🖱 講師の操作 | Zoom とプロジェクターで見せる操作 |
| 👥 参加者 | 手を動かす人への手順。**会場チャットと Zoom チャットの両方に同じ文を貼る** |
| 📺 視聴者 | Zoom 担当が Zoom チャットに貼る。講師は返事を待たない |
| ✅ 確認 | 次へ進む合図。**会場の挙手だけで数える。** 6 割で進む |
| 🆘 詰まったら | 症状と対処 |
| ⏱ | 時間の目安と逃げ道 |

**講師の画面（開始前に開いておく）**
1. ブラウザ: 講師 PDI に `admin` でログイン済み。タブ A = Handson Todo board、タブ B = Automated Test → Tests 一覧を Application「ATF Handson 10」で絞った状態
2. ターミナル 1: `themeB-kit\atf-tests`（空。ここで `claude` を起動する）
3. ターミナル 2: `themeB-kit\todo-app`（37 分にここで `claude` を起動してプロンプト③）
4. 共有しない画面: この原稿、スライド 5 枚、`theme-b/evidence/` のスクリーンショット 3 枚

**共用 PC A/B/C（前日までに講師が用意。3 台とも同じ状態にする）**

| # | 状態 | 確認方法 |
|---|---|---|
| 1 | デスクトップに `themeB-kit` を展開し、`setup.ps1` を通してある（その PC 用の PDI に欠陥版が入っている） | `.\preflight.ps1` が 12 項目すべて [OK] |
| 2 | `now-sdk auth` の alias `mypdi` がその PC 用の PDI を指す | `now-sdk auth --list` |
| 3 | Claude Code にログイン済み（`claude` と打つと入力欄が出る） | 一度 `claude` を起動して `/exit` |
| 4 | ブラウザでその PDI に `admin` でログイン済み。タブ 1 = Handson Todo board、タブ 2 = Automated Test → Tests | 開いた状態でスリープさせない |
| 5 | `atf-tests\src\fluent\atf\` が空、`docs\` が空 | エクスプローラーで見る |
| 6 | 机に「A」「B」「C」の札。札の裏に PDI の URL と admin パスワード | |
| 7 | ATF の実行設定 2 つが true（`setup.ps1` が自動で入れる） | preflight の最後の行 |

共用 PC の PDI は **講師の PDI とは別に 3 つ必要**。お世話係・技術相談の方に PDI を 1 つずつ貸してもらうのが最短（前日までに `setup.ps1` を通す）。借りられない台数分は、その PC を「講師の画面を一緒に見る席」にする。

---

## 1. 開始前 10 分　入室した人から順に準備

🖱 **講師の操作**: Zoom を開き、画面共有を開始（タブ A のボード画面）。会場に来た人へ、入口で次の 2 択を聞く。「自分の PC で事前課題を終えていますか」。終えていない人は共用 PC A → B → C の順に案内し、**2 人目が来たら同じ PC に座らせる**（打つ役・読み役）。

会場チャットと Zoom チャットに次を貼る。

👥
```
【開始前にお願い・自分の PC の方】
1. ご自身の PDI にブラウザでログインしておいてください（休止から起こすため）。
2. themeB-kit フォルダを開き、何もない所で右クリック → 「ターミナルで開く」。
3. 開いたターミナルで  .\preflight.ps1  と打って Enter。
4. 全部 [OK] なら手を挙げてください。[NG] の行があれば講師を呼んでください。
```

👥
```
【共用 PC A/B/C の方】
PC はすでに準備済みです。ブラウザに Handson Todo board が開いています。
2 人 1 台で、片方が「打つ役」、もう片方が「読み役（画面の文字を声に出す）」。
35 分ごろに役を交代します。まだ何も打たなくて大丈夫です。
```

📺（Zoom 担当が貼る）
```
オンラインで環境をお持ちでない方へ。今日は「設計者役」として見てください。
事前にお送りした仕様書（5 行）を手元に開いておいてください。30 分ごろまで内容は伏せてください。
オンラインで事前課題を終えた方は、会場と同じ手順をご自身のペースで進めてください。講師は会場の進み具合で進行します。
```

🖱 **講師の判断（開始 2 分前）**: 会場の挙手を見る。NG の人は共用 PC の空き席か、隣の人との相席へ。**開始時点で環境が動いていない人を待たない。**

🆘
- 「スクリプトの実行が無効」→ `Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned` → `Y` → もう一度 `.\preflight.ps1`。
- 「接続先 [mypdi] が登録されていない」→ `now-sdk auth --add https://devXXXXXX.service-now.com --type basic --alias mypdi`（URL は自分の PDI、`admin`）。2 分で直らなければ共用 PC へ。
- PDI が起きない → 共用 PC の空き席へ。空きがなければ講師の画面を見てもらい、19 分の Checkpoint で合流。
- 共用 PC のブラウザがログアウトしている → 札の裏の URL とパスワードで `admin` ログイン。

---

## 2. 0〜4 分　導入（4 分）

🗣 **言う**
「今日は 50 分で、AI に書かせたテストが本当に品質を守ってくれるのかを確かめます。皆さん一人ひとりの PDI に、同じアプリが入っています。共用 PC の方も、その PC 専用の PDI です。他の人と一切干渉しません。壊しても大丈夫です。皆さんはテスター役です。アプリの画面を見て、AI にテストを書かせます。後半では、皆さん自身が開発者として、そのアプリを直します。オンラインの設計者役の皆さんは仕様書を持っています。テスターの作るテストに見落としがないか、見ていてください。」

🗣 **言う（用語 4 つ。30 秒）**
「言葉を 4 つだけ。ATF は ServiceNow に最初から入っている自動テストの機能で、ボタン一つで動きます。Positive テストは『正しく使えば正しく動く』を確かめるもの。Negative テストは『間違った使い方は拒否される』を確かめるもの。結果は緑が成功、赤が失敗。今日はこの 4 つが分かれば大丈夫です。」

🖱 **講師の操作**: スライド 1（ゴールと役割）を出す。「ServiceNow を触ったことがある人」を挙手で見る。

👥
```
【Claude Code を起動・打つ】themeB-kit\atf-tests フォルダを開き、何もない所で右クリック → 「ターミナルで開く」。
開いたターミナルで  claude  と打って Enter。
「このフォルダを信頼しますか」と出たら Yes。入力欄が出れば OK。まだ何も打たないでください。
（共用 PC の方: デスクトップの themeB-kit の中の atf-tests です）
```

✅ **確認**: 「claude の入力欄が出た人、挙手。」会場の 6 割で進む。出ていない人には講師が席まで行く。**オンラインは待たない。**

🆘
- `claude` が「ログインしてください」→ 指示どおりブラウザでログイン。1 分以上かかるなら講師の画面を見てもらい、19 分の Checkpoint で合流。
- 共用 PC で `claude` がログアウトしていた → 講師が自分のアカウントでログインする（前日に確認しておくのはこのため）。

---

## 3. 4〜8 分　アプリを触る（4 分。参加者に仕様書は渡さない）

🗣 **言う**
「Handson Todo board というアプリです。タスクを作って、Open・In Progress・Done の 3 列で管理する簡単なものです。2 分半、自分の PDI で自由に触ってください。タスクを作る、列を動かす、開く、消す。共用 PC は打つ役が操作し、読み役は何が起きたか声に出してください。」

🖱 **講師の操作**（**空タイトルの保存は絶対に試さない**）
1. タブ A で左上 **All** → `Handson` → **Handson Todo board**。
2. **New** → タイトル `講師のタスク` → **Save**。一覧に出る。
3. ボードに戻り、矢印ボタンで In Progress へ動かす。

👥
```
【アプリを触る】自分の PDI のブラウザで、左上 All → 検索欄に Handson → Handson Todo board。
2 分半、自由に触ってください。タスクを作る、列を動かす、開く、消す。
```

📺（Zoom 担当が貼る）
```
設計者の皆さんへ。テスターはこのあと AI にテストを 3 本書かせます。
「仕様のどの行が抜けそうですか」を予想してチャットに書いてください。30 分ごろに講師が拾います。
```

🗣 **言う（締め）**
「テスター役に質問です。このアプリの今の動きを守るテストを書くなら、何を確かめますか。それをこれから AI に頼みます。」

---

## 4. 8〜19 分　Claude Code でテスト生成 ＋ 講師の実況（11 分）

🗣 **言う**
「Claude Code にテストを 3 本書かせます。文面はチャットに貼るので、そのままコピーして貼り付けてください。5〜8 分かかります。その間、私も同じことを画面でやります。」

👥
```
【プロンプト①】claude の入力欄に、下の文をそのまま貼って Enter。
途中で「ファイルを作成してよいか」「npm run build を実行してよいか」と聞かれたら Yes。
「build が通りました」のような報告が出るまで待つ。deploy はまだしない。
```
続けて貼る:
```
このプロジェクトは ServiceNow Fluent SDK の ATF テスト専用プロジェクトです。
テスト対象は同じインスタンス上の別スコープのアプリ「Handson Todo」です。
スコープ名・テーブル名・画面の URL は CLAUDE.md の「テスト対象アプリ」の節に書いてあります。それを使ってください。
CLAUDE.md のルールに従って、次の 3 本の ATF テストと、それをまとめた TestSuite を src/fluent/atf/ に作ってください。
1. Positive（サーバー）: タイトル付きでレコードを作成でき、state の初期値が open であること
2. Positive（UI）: ボード画面で New を押し、タイトルを入力して Save すると一覧に表示されること
3. 今のアプリの挙動を確認したうえで、もう 1 本、あなたが妥当だと思うテストを追加する
作り終えたら npm run build が通ることを確認してください。deploy はしないでください。
```

🖱 **講師の操作**: ターミナル 1 で `claude` を起動し、同じプロンプト①を貼る。**Zoom 共有をこのターミナルに切り替える。** 動いている間、実況する。

🗣 **実況（進み方に合わせて順番は前後してよい）**

CLAUDE.md を読んでいるとき:
「まず AI がプロジェクトの説明書を読んでいます。ATF の書き方とボタンの名前、そして皆さんそれぞれの PDI に合わせたアプリ名が書いてあります。事前課題のスクリプトが、この説明書を皆さんの会社コードに書き換えてありました。」

ファイルを作り始めたとき（**スライド 2「ATF のテストとは」**）:
「ATF のテストは『ステップ』の並びです。レコードを 1 件作る、値が open か確かめる、画面で New を押す。それぞれが 1 ステップ。今できている `.now.ts` というファイル 1 つが、ServiceNow 画面のテスト 1 つになります。」

3 本目のファイルができたとき（**必ず中身を読み上げる**）:
「3 本目は、AI に『妥当だと思うテストを追加して』と頼んだものです。……（画面を読む）……『〇〇すると〇〇になる』ことを確かめています。今のアプリの動きをそのまま確かめるテストですね。皆さんの 3 本目も似た形のはずです。これでいいかどうか、後で戻ってきます。」

`npm run build` が走っているとき（**スライド 3「Positive と Negative」**）:
「テストには 2 種類あります。正しく使ったら正しく動く、が Positive。間違った使い方が拒否される、が Negative。AI は Positive が得意です。今の動きを見て、その通りに動くことを確かめるテストは上手い。Negative は、何が間違いかを知らないと書けません。この違いを後半で見ます。」

🗣 **待ち時間の問い（生成中、参加者の手が止まっているとき）**
「待っている間に隣の人と 1 つ。このアプリで『やってはいけない操作』は何だと思いますか。30 秒。」（答えは拾わない。35 分の伏線）

📺（Zoom 担当が貼る。14 分ごろ）
```
講師の画面に AI が書いたテストが映っています。仕様書の 5 行と見比べて、
「仕様にあるのに確かめていない行」があれば番号を書いてください。30 分ごろに講師が拾います。
```

✅ **確認**（18 分）: 「『build が通りました』と出た人、挙手。」会場の 6 割で進む。

⏱ 19 分になったら、終わっていない人には次を貼って先へ進む。**待たない。**

🆘
```
【Checkpoint】claude を Esc で止めて構いません。
themeB-kit\atf-tests\src\fluent\atf\ の中にできかけのファイルがあれば全部削除し、
themeB-kit\checkpoint フォルダの中の全ファイルを、その atf フォルダにコピーしてください（スコープ名は事前課題で書き換え済み）。
そのまま次の【送る】へ進めます。
```
- 講師の生成が遅い → 実況を切り上げ、`themeB-kit\checkpoint\t1-insert-open.now.ts` を開いて読み上げに切り替える。講師も Checkpoint で先へ。

---

## 5. 19〜26 分　build → deploy → 自分の PDI で Run Test（7 分。全部緑になる予定）

🗣 **言う**
「できたテストを自分の PDI に送り、ServiceNow の画面からボタンで実行します。一人一台なので、画面を操作するテストも全員が自分の画面で見られます。」

👥
```
【送る・打つ】claude を Esc で一度抜けるか、別のターミナルを atf-tests で開いて、次を順に打つ。
  npm run build
  npm run deploy
「Installed」や「success」が出れば成功。1〜2 分かかります。
```

```
【実行する・見て押す】自分の PDI のブラウザで、左上 All → 検索欄に Automated Test → 「Tests」。
一覧の上の絞り込みで Application 列に  ATF Handson  と入れる。自分のテストが 3 本出ます。出なければ F5。
1 本目の名前をクリック → 右上の「Run Test」→ 小さな画面の「Run Test」。
「Client Test Runner」という新しいタブが自動で開きます。閉じないでください。
元のタブに戻ると結果画面になり、Status が Success（緑）になります。
残り 2 本も同じように実行。3 本とも緑になったら手を挙げてください（オンラインはチャットに「緑3」）。
```

🖱 **講師の操作**（Zoom 共有をブラウザへ。押すたびに名前を声に出す）
1. タブ B（Tests 一覧、Application「ATF Handson 10」）。ライブ生成が失敗していたら「ATF Handson 09」の `Todo: タイトル付きで作成すると state=open になる` を使う。
2. 1 本目を開き、**Run Test** → **Run Test**。
3. 新しいタブを見せる。「これが Client Test Runner。UI テストはこのタブの中で動きます。閉じると止まります。」
4. 結果画面。「上に Status、下に Test Result Items。ステップごとの結果。緑が成功。」
5. UI テストのときは Runner タブを映す。「人間の代わりにブラウザが動いています。皆さんの画面でも同じことが起きているはずです。」

✅ **確認**（25 分）: 会場の挙手を数える。6 割で進む。**共用 PC 3 台の緑を優先して確認する**（席まで見に行く）。

🗣 **言う（締め）**
「全員、緑ですね。ここで質問です。テストが全部通りました。このアプリは正しいと言えますか。少し考えてください。設計者役の皆さんは答えを知っていますね。」

🆘
- `npm run deploy` が赤字 → 先頭 3 行を読んでもらう。`auth`/`401` → `now-sdk auth --list` に `mypdi` があるか。`scopeId` → `.\setup.ps1 -SkipNpm -SkipInstall`（1 分）。
- 「Waiting for a test runner」のまま → ポップアップブロックを許可して再度 Run Test。または All → Automated Test Framework → Client Test Runner を手で開く。
- UI テストだけ動かない → キットのフォルダで `node set-atf-props.mjs mypdi`。動かなければ `sys_properties.list` で 2 つを true。
- UI テストだけ赤（要素が見つからない）→ 「テスト側の問題の可能性が高いです。今は先に進みます」。サーバー側 2 本が緑なら進む。

---

## 6. 26〜30 分　仕様書を配る（4 分。設計者役の返事は待たない）

🗣 **言う**
「オンラインの設計者役の皆さんは、最初から仕様書を持ってテスターのテストを見ていました。Zoom 担当、チャットに『見落とし』の指摘は来ていますか。」

🖱 **講師の操作**: Zoom 担当が **その場で読める指摘があれば 1 件だけ**読む。**なければ待たずに**講師が言う。「『タイトル必須なのに、空欄のテストがない』。設計者役にはずっと見えていて、テスター役には見えなかった。理由は仕様書がなかったからです。今、配ります。」

🖱 **講師の操作**: スライドに仕様書 5 行を出し、両チャットに貼る。

👥
```
【仕様書】
Handson Todo 仕様（抜粋）
1. タスクにはタイトルが必須である。空欄では新規作成も保存もできず、エラーが表示される。
2. タイトルは 100 文字以内である。
3. 状態は Open / In Progress / Done の 3 つである。
4. 状態を Done にすると completed_at に日時が自動で入る。
5. ボード画面ではカードを隣の列にだけ移動できる。
```

```
【仕様書を保存・打つ】themeB-kit\atf-tests\docs フォルダに spec.md という名前のファイルを作り、上の文を貼って保存。
（メモ帳で新規作成 → 貼る → 名前を付けて保存 → ファイル名 spec.md、種類「すべてのファイル」）
保存したら、自分の 3 本目のテストが仕様のどれを確かめているか見てください。
共用 PC の方: ここで打つ役と読み役を交代してください。
```

🗣 **言う**
「3 本目を見てください。AI は『今のアプリの動き』を見て、その通りに動くことを確かめるテストを書きました。アプリに欠陥があれば、欠陥もそのまま正解として写し取られます。これが今日の一番大事な話です。プロダクトを見てテストを作ると、欠陥が正解になる。だからテストは仕様から作る。では仕様を渡して AI に直させます。」

📺（Zoom 担当が貼る）
```
ここからテスターも仕様書を持ちます。次は AI が Negative Test を書きます。「空欄で保存 → エラーになるはず」というテスト。結果を見ていてください。
```

---

## 7. 30〜37 分　テストを仕様に合わせて直す → 赤を出す（7 分）

👥
```
【プロンプト②】atf-tests のターミナルで claude を起動し（claude と打つ）、下の文をそのまま貼って Enter。
聞かれたら Yes。「build が通りました」まで待つ。
```
続けて貼る:
```
docs/spec.md に仕様書を置きました。仕様と今のテストを照らし合わせ、
仕様で禁止されている操作が拒否されることを確認する Negative Test を 1 本追加してください。
既存テストの期待値が仕様と矛盾している場合は仕様に合わせて直してください。
npm run build が通ることを確認してください。deploy はしないでください。
```

```
【送って実行】終わったら
  npm run build
  npm run deploy
自分の PDI の Tests 一覧を F5。新しく増えた Negative のテストを開き、Run Test → Run Test。
今度は Status が Failure（赤）になります。赤になったら手を挙げてください（オンラインは「赤」）。
```

🖱 **講師の操作**: ターミナル 1 で同じことを実行し、Zoom にターミナル → ブラウザの順で映す。赤の結果画面を出す。間に合わなければ「ATF Handson 09」の `Todo: 空タイトルはサーバーで拒否される (Negative)` を Run Test して赤を見せる。

🗣 **言う（赤が出たら。スライド 4）**
「赤が出ました。ここで 4 分類です。テストが赤になる原因は 4 つのどれかです。1 つ目、アプリの欠陥。これが本来見つけたいもの。2 つ目、テストの間違い。3 つ目、環境。テスト用のタブを閉じた、ログインが切れた。4 つ目、データ。他のレコードと混ざった。」

🖱 **投票（唯一の投票。会場は指、Zoom は投票機能。20 秒）**
🗣 「この赤の原因は 4 つのどれですか。会場の方は指で番号を。Zoom の方は投票に。」会場の指の数を見て、多い番号を言う。**Zoom の結果は待たず、Zoom 担当が後で読み上げる。**

🗣 **言う**
「確かめましょう。結果画面の下、Test Result Items。赤いステップをクリックすると Output という欄があります。」

🖱 **講師の操作**: 赤いステップをクリックし、Output の「Inserted record ... when insert was expected to fail」を読む。

🗣 **言う**
「『レコードが作成された』とあります。テストは『空欄なら保存できないはず』と言い、アプリは保存してしまった。1 番、アプリの欠陥です。テストがアプリの仕様違反を見つけました。」

✅ **確認**（36 分）: 会場の挙手。共用 PC 3 台を優先して見る。

🆘
- Negative が緑になる → その人のテストが「保存できること」を期待している。「AI がまた今の動きを正解にした例です」と紹介し、次を貼る。
  ```
  【Solution】themeB-kit\solution の t4-empty-title-server.now.ts だけを atf-tests\src\fluent\atf\ にコピー
  （todo-suites.now.ts はコピーしない）。npm run build → npm run deploy → Tests 一覧を F5 → 「空タイトルはサーバーで拒否される (Negative)」を Run Test。
  ```
- 37 分を過ぎている → 10 章の捨て順 3 番。Solution のコピーを全員に貼り、プロンプト②を省く。

---

## 8. 37〜45 分　参加者自身がアプリを直す → 再テスト（8 分。案C の見せ場）

🗣 **言う**
「テストが欠陥を見つけました。直すのは開発者の仕事です。今日は皆さん自身が直します。AI に渡すのは 2 つ。守られていない仕様の 1 行と、赤くなったテストの Output。アプリのソースはキットの todo-app フォルダにあります。他の人の環境には何も起きません。」

👥
```
【直す・打つ】themeB-kit\todo-app フォルダを開き、何もない所で右クリック → 「ターミナルで開く」。
（atf-tests ではありません。アプリ本体のフォルダです）
claude と打って Enter。「信頼しますか」は Yes。
下のプロンプト③をそのまま貼って Enter。聞かれたら Yes。「build が通りました」まで待つ（3〜4 分）。
```
続けて貼る（**プロンプト③**）:
```
このプロジェクトは ServiceNow Fluent SDK で作ったアプリ「Handson Todo」です。
次の仕様が守られていません。
  仕様 1: タスクにはタイトルが必須である。空欄では新規作成も保存もできず、エラーが表示される。
ATF の Negative Test「空タイトルで insert すると失敗するはず」が赤になりました。Output は次のとおりです。
  Inserted record ... when insert was expected to fail
サーバー側・画面側・テーブル定義の 3 か所で仕様 1 が守られるように直してください。
- サーバー側: Business Rule のスクリプト（src/server/business-rules/validate-title.ts）で、前後の空白を除いたタイトルが空なら
  gs.addErrorMessage('タイトルは必須です') を出し、current.setAbortAction(true) で保存を中止する
- 画面側: 作成フォーム（src/client/components/TodoForm.tsx）で、タイトルが空のまま Save されたら
  'タイトルは必須です' をエラーとして表示し、サーバーに送らない
- テーブル定義: src/fluent/tables/todo-item.now.ts の title を mandatory: true にする
既存の動き（空白の前後を整える処理、他の列、デモデータ）は変えないでください。
終わったら、変更したファイルと変更点を短く一覧にして見せてください。
npm run build が通ることを確認してください。deploy はしないでください。
```

🖱 **講師の操作**（ターミナル 2 を Zoom に映す）: 講師も `themeB-kit\todo-app` で `claude` を起動し、同じプロンプト③を貼る。動いている間、実況する。

🗣 **実況**

`validate-title.ts` を書き換えたとき（**必ず画面で読む**）:
「サーバー側です。ここが本命。`trimmed === ''`、空白を取ったタイトルが空なら。`gs.addErrorMessage('タイトルは必須です')`、エラーを出して。`current.setAbortAction(true)`、保存そのものを止める。この 3 行が欠陥版にはありませんでした。」

`TodoForm.tsx` と `todo-item.now.ts` を書き換えたとき:
「画面側の守りと、テーブル定義の `mandatory: true`。合わせて 3 か所、差は十数行。テストがなければ誰も気づきませんでした。」

`npm run build` が走っているとき:
「なぜサーバー側が本命か。画面のチェックだけだと、API やスクリプトから入れたレコードは素通りします。だから保存の直前にサーバーでもう一度確かめる。」

⏱ **41 分の判断（この原稿で一番大事な分岐）**: 「『build が通りました』と出た人、挙手。」会場の半分に届いていなければ、**全員に予備を貼って切り替える**（個別に待たない）。届いていれば、出ていない人にだけ予備を貼る。

🆘（41 分の予備）
```
【予備・打つ】claude を Esc で止め、themeB-kit フォルダ（todo-app の 1 つ上）を「ターミナルで開く」→
  .\fix.ps1
  cd todo-app
  npm run build
  npm run deploy
fix.ps1 は同じ 3 ファイルの完成版を上書きします。中身は AI が書こうとしていたものと同じです。
```
「AI の修正は時間がかかることがあるので、用意してある同じ修正を入れます」と言う。

👥
```
【送る・打つ】claude を Esc で抜けるか、別のターミナルを todo-app で開いて
  npm run build
  npm run deploy
「Installed」や「success」が出るまで待つ（1 分ほど）。
```

👥
```
【再テスト・見て押す】自分の PDI の Tests 一覧で
1. Negative のテストを開いて Run Test → Run Test。今度は緑（Success）になるはず。
2. 続けて、最初の Positive のうちサーバー側の 1 本（タイトル付きで作成 → open）も実行。緑のままのはず。
2 本とも緑になったら手を挙げてください（オンラインは「緑緑」）。
```

📺（Zoom 担当が貼る）
```
参加者が AI に指示を出して、自分の手で修正版を入れています。設計者の皆さんは、仕様書 1 番以外の 4 行で「今のうちに書くべきテスト」を番号で書いてください。終了後にまとめて共有します。
```

🗣 **言う（会場の挙手が半分を超えたら）**
「Negative が緑になった。それだけでは足りません。Positive が緑のままであることも確認しました。直したつもりで別の所を壊していないか、回帰確認と言います。テストが自動になっていると、これがボタン 1 つでできます。皆さんは今日、AI にテストを書かせて欠陥を見つけ、AI に指示して直し、再テストで確かめる、この一巡りを一人で終えました。皆さんがしたのは『何を渡すか』を決めることだけです。」

⏱ 実測（2026-09-16）: プロンプト③ 116 秒、build 13〜23 秒、deploy 17〜20 秒。参加者は Yes を押す分だけ長くなる。**44 分になったら再テストは Negative 1 本だけにする。**

🆘
- `fix.ps1` が「setup.ps1 がまだ実行されていません」→ 展開し直したフォルダで打っている。事前課題で使った `themeB-kit` を開き直す。
- deploy が `scopeId` で失敗 → `.\setup.ps1 -SkipNpm`（1 分半）。時間がなければ「講師の画面で結果を見てください」。
- 再テストしても Negative が赤 → deploy 完了前に押している。30 秒待って再度。それでも赤で Output が「Inserted record」なら `validate-title.ts` に `setAbortAction` があるか見る。無ければ `fix.ps1`。
- Positive が赤 → AI が既存の動きを壊した。「回帰確認が要る場面そのものです」と紹介し、`fix.ps1` で上書きして再 deploy。
- 講師の deploy が失敗 → 「修正版は事前に確認済みです」と言い、`theme-b/evidence/suite-result-fix-5pass.png` を出す。参加者側が成功していれば見せ場は成立する。

---

## 9. 45〜48 分　振り返り（3 分）

🖱 **講師の操作**: 両チャットに 1 問だけ貼る。

👥
```
【振り返り】1 行で書いてください。1 分。
・プロダクトを見て作ったテストで確かめられなかったものは何でしたか。自分で直して再テストしたとき、何を感じましたか。
```

📺（Zoom 担当が貼る）
```
【振り返り】1 行。自分の現場で AI にテストを書かせるなら、最初に何を渡しますか。
```

🗣 **言う（1 分後）**: **会場の書き込みを 1 件だけ**読み上げ、一言返す。Zoom の書き込みは Zoom 担当が終了後にまとめる。

🗣 **締めのセリフ（スライド 5）**
「まとめます。AI はテストを速く書けます。今日 3 本を 8 分で書きました。でも、何が正しいかを決めるのは仕様です。AI はそれを知りません。設計者役には最初から答えが見えていて、テスター役には 25 分見えませんでした。その差を作ったのは紙 1 枚です。そして皆さんは、テストが見つけた欠陥を自分の手で直し、再テストで確かめました。持ち帰ってほしいのは 3 つ。1 つ、仕様を先に渡す。2 つ、Negative を必ず頼む。3 つ、赤が出たら 4 分類で考える。」

🗣 **言う（10 秒）**
「今日作ったテストと直したアプリは、皆さんの PDI と themeB-kit に残っています。共用 PC の方には、終了後にキットと資料の URL を送ります。月曜にもう 1 本 Negative を足してみてください。アンケートの URL をチャットに貼ります。」

---

## 10. 48〜50 分　講師の一言（2 分。インターンとして）

**60 分版の 5 分を 2 分に。学んだこと 1 つ + 感謝。反省点は言わない（終了後の資料に書く）。**

🗣
「最後に 2 分だけ、インターン生として。私は月曜に ServiceNow SDK も ATF も初めて触りました。今日のアプリも、テストも、皆さんの PDI に入れたキットも、この 4 日間で Claude Code と一緒に作りました。一番学んだのは、AI は『今の動き』を正解にする、ということです。今日の 3 本目のテストと同じことを、準備中に何度も経験しました。仕様を文章にして渡した瞬間に出力が変わる。何が正しいかを決めるのは人の仕事だと、体で分かりました。」

「お世話係の【名前】さん、技術相談の【名前】さん、PDI を貸してくださった【名前】さん、事前課題に時間を割いてくださった皆さん、ありがとうございました。」

⏱ 49 分 30 秒で必ず終える。押していたら「一番学んだのは」の 1 文と感謝だけ。

---

## 11. 時間が押したときの捨て順（50 分版）

1. 9 章の振り返りを 1 分に（書き込みを読まず、締めのセリフに入る）。
2. 10 章を 1 分に（感謝だけ）。**ゼロにはしない。**
3. 7 章のプロンプト②をやめ、Solution の T4 をコピーして deploy → Run Test だけにする（3 分短縮）。
4. 8 章のプロンプト③を最初から `fix.ps1` にする（3 分短縮。参加者が自分で直す体験は残る）。
5. 8 章の再テストを Negative 1 本だけにする。
6. 3 章のアプリを触る時間を 1 分にする。
7. 8 章の参加者による修正を全部やめ、講師の実演だけにする。案C の見せ場なので、4・5 番でも間に合わないときだけ。

**残さないもの（この版で最初から削ってある）**: 投票 1・3、設計者役の発言待ち、振り返りの複数読み上げ、講師の反省点。

逆に余ったら: 8 章のあとに、設計者役が挙げた「書くべきテスト」から 1 つ選び、参加者に Negative をもう 1 本書かせる。

---

## 12. 困ったとき早見表（50 分版で増えた行 + 主要行）

| 症状 | まず見る | 対処 |
|---|---|---|
| 自前の環境がない人が 7 人以上来た | 共用 PC 3 台 × 2 人 = 6 人が上限 | 7 人目以降は空いている席の 3 人目（読み役）か、講師の画面を見る席。「オンライン視聴者と同じ扱い」と伝える |
| 共用 PC の PDI が休止していた | ブラウザのタブ | 札の裏の URL でログインし直す。起きるまで 2〜3 分。その間は講師の画面を見てもらう |
| 共用 PC の `claude` がログアウトしていた | ターミナル | 講師のアカウントでログインする。1 分で直らなければ Checkpoint で合流 |
| 共用 PC で前の組の成果物が残っている（テストが 3 本以上ある） | Tests 一覧 | 前日の準備漏れ。そのまま進め、Application の絞り込みで自分の分を見分ける |
| 事前課題をしていない人が自分の PC で来た | | 共用 PC の空き席へ。空きがなければ相席（読み役）。**当日に setup.ps1 を走らせない**（5 分かかる） |
| オンライン参加者が遅れている | Zoom チャット | 待たない。Zoom 担当が Checkpoint / Solution / fix.ps1 の文を個別に貼る |
| preflight で「接続先 [mypdi] が登録されていない」 | `now-sdk auth --list` | `now-sdk auth --add https://devXXXXXX.service-now.com --type basic --alias mypdi`。2 分で直らなければ共用 PC へ |
| `npm run deploy` で `scopeId` がない | `now.config.json` | `.\setup.ps1 -SkipNpm -SkipInstall`（atf-tests）/ `.\setup.ps1 -SkipNpm`（todo-app） |
| deploy で「Unable to install application as application was null」 | 接頭辞 | `.\setup.ps1` をやり直す |
| Run Test で Runner が開かない | ポップアップブロック | 許可して再度。または All → Automated Test Framework → Client Test Runner |
| UI テストが Pending のまま | `sn_atf.runner.enabled` | キットのフォルダで `node set-atf-props.mjs mypdi` |
| Negative が緑になる | テストが「保存できる」を期待 | Solution の T4 を渡す |
| プロンプト③が 41 分までに終わらない | | 全員 `fix.ps1` に切り替える（迷わない） |
| 修正後に Positive が赤 | AI が既存の動きを壊した | 回帰確認の実例として紹介し、`fix.ps1` で上書き |
| Zoom の画面共有が止まる | | 会場を優先して進める。Zoom 担当がチャットで実況し、復旧後に共有を再開 |

---

## 13. 当日チャットに貼る文の一覧（コピー元）

| 時刻 | 見出し | 本文の場所 |
|---|---|---|
| 開始前 | 【開始前にお願い・自分の PC の方】【共用 PC A/B/C の方】 | 1 章 |
| 0 分 | 【Claude Code を起動・打つ】 | 2 章 |
| 4 分 | 【アプリを触る】 | 3 章 |
| 8 分 | 【プロンプト①】+ 本文 | 4 章 |
| 19 分（予備） | 【Checkpoint】 | 4 章 🆘 |
| 19 分 | 【送る・打つ】【実行する・見て押す】 | 5 章 |
| 26 分 | 【仕様書】【仕様書を保存・打つ】 | 6 章 |
| 30 分 | 【プロンプト②】+ 本文、【送って実行】 | 7 章 |
| 35 分（予備） | 【Solution】 | 7 章 🆘 |
| 37 分 | 【直す・打つ】+ プロンプト③本文 | 8 章 |
| 41 分（予備） | 【予備・打つ】（`fix.ps1`） | 8 章 🆘 |
| 42 分 | 【送る・打つ】 | 8 章 |
| 43 分 | 【再テスト・見て押す】 | 8 章 |
| 45 分 | 【振り返り】 | 9 章 |
| 48 分 | アンケート URL | 9 章末 |

Zoom 担当が貼る 📺 の文は 1・3・4・6・7・8・9 章。前日に `theme-b/handout/zoom-staff-memo.md` と一緒に渡し、「講師は Zoom の返事を待たないので、返事は終了後にまとめて共有する」と伝えておく。

---

## 14. 60 分版との差分（一覧）

| 60 分版 | 50 分版 | 理由 |
|---|---|---|
| 導入 5 分 / アプリ 5 分 | 4 分 / 4 分 | 用語説明と操作は同じ。挙手の待ちを短く |
| 生成 12 分 / 実行 8 分 | 11 分 / 7 分 | Claude Code の実測（5〜8 分）に合わせて余白を削る |
| 設計者の出番 5 分（発言待ち） | 仕様書配布 4 分（返事は待たない） | ハンズオン優先 |
| 赤 8 分（投票 2 は Zoom） | 赤 7 分（投票は会場の指。Zoom は並行） | Zoom の集計を待たない |
| 修正 8 分（47 分で予備判断） | 修正 8 分（**41 分で全員判断**） | 遅れた人を個別に待たず、一斉に切り替える |
| 振り返り 4 分 + 投票 3 | 振り返り 3 分、投票なし | |
| 講師の学びと反省 5 分 | 講師の一言 2 分 | 反省点は終了後の資料へ |
| 参加者 = 事前課題を終えた人だけ | + 会場の共用 PC 3 台（2 人 1 台） | 環境がない人もハンズオンに入れる |
| ✅ は会場 + Zoom で 8 割 | ✅ は **会場の挙手だけ**で 6 割 | ハンズオン優先、時間厳守 |
